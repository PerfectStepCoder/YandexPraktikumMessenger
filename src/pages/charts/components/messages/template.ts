const template = `
<div class="chat-area">
    <div class="chat-messages">
    </div>

    <!-- Поле для ввода сообщения -->
    <div class="message-input">
        {{ enterMessage }}
        {{ sendMessage }}
    </div>
</div>
`;

export default template
