const template = `
<div class="register-form">
    <h2>Register</h2>
    <form id="mainForm">
        {{ firstName }}
        {{ secondName }}
        {{ displayName }}
        {{ login }}
        {{ password }}
        {{ email }}
        {{ phone }}
        {{ saveButton }}
    </form>
</div>
`;

export default template
