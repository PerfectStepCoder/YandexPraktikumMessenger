import Button from "./Button"; // Основной компонент
import ButtonProps from "./Button"; // Интерфейс свойств (если используется)

export { Button, ButtonProps }; // Экспортируем всё необходимое
export default Button; // Экспорт по умолчанию